#include <iostream>

/*
int N;

int questionA(int last, int count) {
	if (count == N) return last;
	return questionA(last - 2, count + 1);
}

int questionB(int last, int count) {
	if (count == N) return last;
	return questionB(last * 2, count + 1);
}

int main_208a() {
	std::cin >> N;
	std::cout << questionA(3, 1) << "\n" << questionB(-1, 1);
	return 0;
}
*/